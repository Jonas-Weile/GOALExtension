use passParameterToListall as mas.
use testUntilResult as module.
use result as knowledge.

test testUntilResult with
	pre{ not(bel(result(_))) }
	post{ bel(result(ok)) }

passParameterToListall {
	do testUntilResult.
}