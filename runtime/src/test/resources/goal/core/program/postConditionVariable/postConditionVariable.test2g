use postConditionVariable as mas.
use postConditionVariable as module.
use result as knowledge.

test postConditionVariable with
	pre{ not(bel(result(_))) }
	post{ bel(result(ok)) }

postConditionVariable {
	do postConditionVariable.
}